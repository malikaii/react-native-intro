import { StyleSheet } from "react-native";

export const searchStyle = StyleSheet.create({
    container: {
        padding: 10,
    },
    searchInput: {
        backgroundColor: 'white',
        height: 40,
        padding: 10,
        borderRadius: 10
    }
})